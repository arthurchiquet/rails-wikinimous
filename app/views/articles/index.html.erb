<div class='container w-50 rounded' style="background-color: #E6E6FA;">
  <% @articles.each do |article| %>
    <h2><%= link_to article.title, article_path(article), style:"text-decoration: none" %></h2>
    <p>- <%= article.content %> -</p>
    <hr>
  <% end %>
</div>
